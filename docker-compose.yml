services:
  app:
    build: .
    container_name: mevzuat_ai_app
    ports:
      - "8003:8003"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - mevzuat_net


networks:
  mevzuat_net:
    driver: bridge